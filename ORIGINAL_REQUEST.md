# Original User Request

## 2026-09-02T21:36:29Z

Полномасштабная команда аудита (Full Team): параллельный построчный аудит всех 28 лекций, JS/PWA модулей, CSS и Python-инструментов независимыми аудиторами и ревьюерами.

Выполнить исчерпывающий сквозной построчный аудит (line-by-line verification) всех файлов репозитория AI-Course: проверить каждую строку в 28 лекциях, клиентских JS-модулях, PWA Service Worker, CSS-стилях и Python-скриптах на отсутствие скрытых регрессий, дефектов, невалидных формул или неиспользуемого кода, подтвердив 100% стабильность всех автоматизированных проверок.

Working directory: c:\Users\egorribun\Documents\AI-Course
Integrity mode: development

## Requirements

### R1. Построчный аудит контента 28 лекций и HTML
- Построчно проверить все 28 файлов лекций (`lectures/*.html`), `index.html` и `exam.html` на валидность разметки, закрытие всех тегов, отсутствие битых ссылок и корректность инварианта 8 секций.
- Проверить каждую математическую формулу LaTeX на корректность синтаксиса и рендеринга в MathJax 3.
- Проверить каждый фрагмент кода Python через `ast.parse` на отсутствие синтаксических ошибок.
- Проверить орфографию и единообразие буквы «ё» во всех текстовых блоках.

### R2. Построчный аудит и верификация кода (JS, CSS, Python)
- Провести аудит каждой строки клиентских скриптов (`js/tracker.js`, `js/exam.js`, `js/lecture.js`, `js/app.js`, `sw.js`): отсутствие console.log, строгая обработка исключений, JSDoc-типы (`tsc --checkJs` = 0 ошибок).
- Проверить таблицу стилей `style.css` через `stylelint` на отсутствие невалидных свойств и селекторов.
- Проверить инструменты `tools/build_exam_data.py` и `tools/export_anki.py` на чистоту кода и сохранение строго 100% line & branch coverage в pytest.

### R3. Сквозной контрольный прогон всех тест-сьютов
- Выполнить полный запуск всех уровней тестирования: `pytest` (100% cov), `npm test` (100% JS cov), `tsc`, `stylelint`, `ruff check`, `ruff format`, `node tests/adversarial_harness.cjs` и 5-тировый мастер-раннер `run_all_tests.py`.

## Acceptance Criteria

### Контентная верификация
- [ ] 0 битых относительных ссылок или якорей во всех 30 HTML-страницах.
- [ ] 100% формул LaTeX сбалансированы и не вызывают ошибок синтаксиса.
- [ ] Все Python-сниппеты парсятся модулем `ast` без ошибок.
- [ ] Все 296 вопросов «Препод спросит» и 170 микро-задач доступны и синхронизированы в `js/exam_data.js` и `anki_decks/`.

### Программная и тестовая верификация
- [ ] `uv run pytest tests/ -v --cov=tools --cov-branch --cov-fail-under=100` завершается с кодом 0 (100% line & branch coverage).
- [ ] `npm test` завершается с кодом 0 и подтверждает 100% line coverage по клиентским модулям JS.
- [ ] `npm run check:types` (`tsc --noEmit --checkJs`) завершается с 0 ошибок.
- [ ] `npm run lint:css` (`stylelint style.css`) завершается с 0 ошибок.
- [ ] `uv run ruff check .` и `uv run ruff format --check .` завершаются с 0 замечаний.
- [ ] `uv run python tests/run_all_tests.py` завершается с результатом 100% (314/314 passed).
- [ ] PWA Service Worker корректно регистрируется и обеспечивает оффлайн-доступность.
